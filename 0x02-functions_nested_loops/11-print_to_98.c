#include <stdio.h>
#include "main.h"

/**
 * print_to_98 - prints to 98
 * @n: number to be printe 98
 * Return: all numbers to 98
 */
void print_to_98(int n)
{
	if (n > 98)
	{
		for(x; x <= 98; x++)
		{
			putchar(x);
			putchar(',');
			putchar(' ');
		}
	}
	else if (x <= 98)
	{
		for (x; x <=98; x++)
		{
			putchar(x);
			putchar(',');
			putchar(' ');
		}
	}
}
