#include <stdio.h>

/**
 * main -Entry point uses the puts function
 *
 * Return:Always 0 (Success)
 */
int main(void)
{
	char *puzzle = "\"Programming is like building a multilingual puzzle";
	
	puts(puzzle);
	return (0);
}
